.venv/
__pycache__/
*.pyc
.DS_Store
.claude-journal-state.json

# Symlinks created by reader/serve.sh — point to $CLAUDE_JOURNAL_DATA_DIR.
# Never tracked in the app repo; user data lives in the data repo.
/data
/memory
/raw

# Local config overrides (user-specific configs live in the data repo)
config/topics.yaml
config/hard_exclude.yaml
config/sources.yaml

